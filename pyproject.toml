[build-system]
requires = ["setuptools>=42.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=fun_chain"
testpaths = [
    "tests"
]

[tool.mypy]
mypy_path = "fun_chain"
check_untyped_defs = true
